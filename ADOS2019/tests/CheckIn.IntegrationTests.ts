import * as path from "path";
import * as assert from "assert";
import * as ttm from "azure-pipelines-task-lib/mock-test";
import * as nu from "azure-devops-node-utilities";

describe("CheckIn.IntegrationTests", function () {

	before(function () {

	});

	after(() => {

	});

	describe("#pushGitChanges()", function () {

		it.skip("Likely unsafe for integration testing...", async function () {
			// Arrange
			this.timeout(3000);

			// Act


			// Assert

		});

	});

	describe("#checkInTfvcChanges()", function () {

		it.skip("Likely unsafe for integration testing...", async function () {
			// Arrange
			this.timeout(3000);

			// Act


			// Assert

		});

	});

});