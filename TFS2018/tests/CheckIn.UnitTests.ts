import * as path from "path";
import * as assert from "assert";
import * as ttm from "azure-pipelines-task-lib/mock-test";
import * as nu from "azure-devops-node-utilities";

describe("CheckIn.UnitTests", function () {

	before(function () {

	});

	after(() => {

	});

	describe("#pushGitChanges()", function () {

		it.skip("Need to design a mock test that can assert against gitApi.createPush()", async function () {
			// Arrange
			this.timeout(3000);

			// Act


			// Assert

		});

	});

	describe("#checkInTfvcChanges()", function () {

		it.skip("Need to design a mock that can assert against tfvcApi.createChangeset().", async function () {
			// Arrange
			this.timeout(3000);

			// Act


			// Assert

		});

	});

});